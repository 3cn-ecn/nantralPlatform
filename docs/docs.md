# Run the documentation website in local

## Installation

```bash
sudo apt-get install rubygems ruby-dev
sudo gem install jekyll bundler
cd docs/
bundle install
```

## Run the server

```bash
bundle exec jekyll serve
```